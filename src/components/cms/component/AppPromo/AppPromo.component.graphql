fragment AppPromoData on AppPromo {
  Title
  # Description {json}
  # Image { ...PublicImageReferenceData }
  # QRCodeImage { ...PublicImageReferenceData }
  # QRCodeText
  # AppStoreButtonLink
  # GooglePlayButtonLink
  # AppStoreImage { ...ReferenceData }
  # GooglePlayImage { ...ReferenceData }
  # CTAButton
}