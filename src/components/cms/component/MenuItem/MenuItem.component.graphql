fragment MenuItemData on MenuItem {
  label: MenuItemLabel
  link: MenuLink { ...LinkData }
  # MenuSummary
  icon: MenuIcon { ...PublicImageReferenceData }
  # IsExternalLink
  # Class
  # SubMenu { ...IContentListItem }
  # VideoURL { ...ReferenceData }
  __typename: _type
}