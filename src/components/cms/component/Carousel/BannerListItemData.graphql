fragment BannerListItemData on Banner {
  title: BannerTitle
  # BannerType
  # BannerSubTitle
  # BannerDescription
  media: BannerMedia { ...PublicImageReferenceData }
  # BannerMobileMedia { ...ReferenceData }
  buttonLabel: BannerLinkLabel
  buttonLink: BannerLink { ...LinkData }
  icon: BannerIcon { ...PublicImageReferenceData }
  # BannerVideoURL
  # BannerStyle { ...DictionaryBlockPropertyData }
}