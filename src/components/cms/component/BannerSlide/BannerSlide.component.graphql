fragment BannerSlideData on BannerSlide {
  Title
  # TitleStyle
  # Description
  # Image { ...ReferenceData }
  # SlideVideo { ...ReferenceData }
  # MobileImage { ...ReferenceData }
  # Iframe
  # Buttons { ...IContentListItem }
  # Aligment
}